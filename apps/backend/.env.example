PORT=3001
NODE_ENV=development
JWT_SECRET=your-secret-key-change-this-in-production
DATABASE_PATH=./data/maifead.db
FEED_FETCH_INTERVAL=*/15 * * * *

# Admin Configuration
# Set the email address that will automatically become admin when signing up
# For production, change this to your actual email address
ADMIN_EMAIL=test@maifead.com

# App Configuration
# The public URL where your app is hosted (used for invite links)
# For development, this defaults to http://localhost:5173
# For production behind a proxy, set this to your domain (e.g., https://maifead.domm.media)
BASE_URL=http://localhost:5173
